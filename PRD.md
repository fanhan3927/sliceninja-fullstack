# SliceNinja（《水果忍者》Web 复刻）PRD

## 产品概述

SliceNinja 是一款可在浏览器游玩的经典《水果忍者》复刻：玩家用鼠标 / 触控滑动切割飞出的水果得分，避开炸弹；游戏随 Level 提升加快出果节奏、提高飞行速度并引入更多炸弹。游客可即开即玩；登录用户可保存战绩、查看历史局与全球排行榜。目标用户为休闲玩家与需要演示「画面 + 玩法逻辑 + 动态难度 + 多媒体加载」的全栈 / 前端工程验证场景。

一句话：在网页上完整复刻水果忍者的切割手感、关卡节奏与音效，并用后端持久化分数与难度配置。

## 核心功能

1. **画面与基础玩法**
   - 全屏深色道场背景（木质案板 / 夜色道场风格），顶部 HUD：分数、连击、生命（叉号）、当前 Level、最高分。
   - 水果从底部或两侧抛物线抛出，受重力下落；种类至少包含：西瓜、苹果、橙子、香蕉、猕猴桃、菠萝；另有炸弹。
   - 玩家按下并拖动形成刀光轨迹（白色/金色渐隐刀痕 + 少量粒子）。
   - **切割判定**：刀光折线与水果碰撞圆（或椭圆）相交且滑动速度超过阈值即判定切开。切开后果实分成两瓣并向两侧飞出，带果汁溅射粒子。
   - 漏切水果落地记一次 Miss（最多 3 次 Miss，第三次 Game Over）。切开炸弹立即 Game Over（或扣两条命，以配置为准，默认立即结束）。
   - 连击：短时间窗口内连续切开 ≥3 个水果触发 Combo，额外加分并播放连击音效 / 屏幕中央 Combo 字样。

2. **动态难度机制（必须完整、可配置、可验证）**
   - 以 **Level** 为难度主轴。Level 提升条件：累计切开水果数达到阈值，或当前 Level 存活时长达到阈值（默认：每切开 `8 + Level * 2` 个水果升一级）。
   - 随 Level 递增的数值（必须在代码中集中配置，禁止魔法数散落）：
     - 出果间隔 `spawnIntervalMs`：Level 1 = 1400ms，之后每级减少 80ms，下限 420ms。
     - 单波水果数量 `fruitsPerWave`：Level 1 = 1，Level 3+ 有概率 2 连抛，Level 6+ 有概率 3 连抛。
     - 初速度 `throwSpeed`：基础 7.2，每级 +0.35，上限 14。
     - 重力 `gravity`：基础 0.18，每级 +0.012，上限 0.38。
     - 炸弹出现概率 `bombChance`：Level 1 = 0，Level 2 = 0.08，之后每级 +0.035，上限 0.32。
     - 水果半径略微减小（更难点切）：基础 42px，每级 -1.2px，下限 28px。
     - 连击时间窗口略缩短：基础 900ms，每级 -30ms，下限 480ms。
   - HUD 在升级瞬间播放 Level Up 提示与短音效；出果节奏必须「肉眼可见」变快。
   - 难度参数可由服务端 `GameConfig` 覆盖（登录管理员或种子数据），客户端启动时拉取并缓存。

3. **音频多媒体（必须按约定路径加载，失败有降级）**
   - 背景音乐循环播放，可静音；默认进入对局后自动播放（需处理浏览器自动播放策略：首次用户手势后解锁 AudioContext）。
   - 切割成功：切片音效（可 2～3 个变体随机）。
   - 切中炸弹：爆炸音效。
   - 漏切：短促 miss 音。
   - Combo / Level Up / Game Over：独立音效。
   - 资源约定路径（coding agent 必须按此放置并加载，可用免版权占位音频）：
     - `/public/audio/bgm.mp3`
     - `/public/audio/slice-1.mp3`
     - `/public/audio/slice-2.mp3`
     - `/public/audio/slice-3.mp3`
     - `/public/audio/bomb.mp3`
     - `/public/audio/miss.mp3`
     - `/public/audio/combo.mp3`
     - `/public/audio/level-up.mp3`
     - `/public/audio/game-over.mp3`
   - 图像资源约定：
     - `/public/images/fruits/{watermelon,apple,orange,banana,kiwi,pineapple}.png`
     - `/public/images/fruits/{name}-half-a.png` / `{name}-half-b.png`（若无半切图，允许用程序化对切原图）
     - `/public/images/bomb.png`
     - `/public/images/dojo-bg.jpg`
     - `/public/images/slash-particle.png`（可选，也可用 Canvas 粒子）
   - 加载器：进入游戏前显示 Loading 进度（图片 + 音频 decode），全部 ready 或超时降级后才允许 Start。音频加载失败不得阻断开局，仅静音并在设置里提示。

4. **账号、战绩与排行榜（全栈）**
   - 游客：可完整游玩，局结束可选择「保存成绩」（跳转登录）或放弃。
   - 注册用户：每局结束后自动提交 `GameSession`（分数、最高连击、Level、切开数、漏切数、是否炸死、时长）。
   - 个人主页：最高分、总场次、总切开数、最近 20 局。
   - 全球排行榜：按最高分 Top 50；并列按最早达成时间。
   - 可选成就：首胜、连击 5/8/12、达到 Level 5/10、累计切开 100 个水果。

5. **设置与暂停**
   - 静音 BGM / SFX 开关（localStorage + 登录用户同步偏好）。
   - 暂停（暂停物理循环与出果，BGM 降低音量）。
   - 重新开始、返回大厅。

## 用户角色与流程

- **未登录访客**：打开首页 → 看玩法说明与排行榜预览 → Start 进入加载 → 游玩 → Game Over 展示本局数据 → 可注册以保存。
- **普通用户**：注册 / 登录 → 大厅（最高分、开始游戏、排行榜、历史） → 对局 → 自动存档 → 查看历史与成就。
- **管理员（种子账号）**：登录后可进入 `/admin/config` 调整难度曲线 JSON 并发布，客户端下次开局拉取。

关键路径：

1. 访问首页 → 点击「开始切割」→ 资源加载 → 第一局。
2. 切割水果得分 / 漏切 / 切炸弹 → Level 提升导致出果变快 → Game Over。
3. 登录用户自动 `POST /api/sessions` → 排行榜更新。
4. 管理员更新 `GameConfig` → 新对局使用新数值。

## 非功能需求

- 桌面与移动端均可玩（触控滑动必须可用，防止页面滚动）。
- 游戏循环目标 60fps；对象池管理水果与粒子，避免每帧分配过多。
- 安全：分数提交需登录会话；服务端校验分数合理性（时长、切开数与分数公式一致，偏差过大拒绝）。
- 无障碍：设置页可键盘操作；游戏画布提供简短文字说明。
- 本地一键运行：`npm install && npx prisma migrate dev && npm run dev`。

## 设计要求

- 视觉目标：经典忍者道场，暗木纹 + 暖色灯笼光，水果高饱和、刀光锐利，切开瞬间有停顿感（hit-stop 20～40ms）。
- 字体：标题书法/衬线感（可用 `Noto Serif SC` + `Geist`），HUD 数字等宽。
- 响应式：桌面 canvas 最大 1280×720 居中；移动端竖屏满宽，HUD 压缩。
- 深色主题全局；按钮大、少文字、强反馈。
- 不使用侵权的官方《水果忍者》商标素材与原版音频；命名 SliceNinja，素材用原创 / 免版权占位。

## 验收标准（对应验证点）

| 验证点 | 通过标准 |
| --- | --- |
| 画面与基础玩法 | 水果抛物线生成；刀光切割判定稳定；切开有分瓣+粒子；切割音效触发 |
| 动态难度 | Level 升级可见；spawnInterval / 速度 / 炸弹率按表变化；数值集中在 config |
| 多媒体 | 按上述路径加载 BGM + 切片音；Loading 显示进度；自动播放策略处理后能响 |
| 全栈 | 注册登录、局记录入库、排行榜、配置可被服务端覆盖 |
| 代码可生成性 | 文档与分步 Prompt 可让 coding agent 按序落地，无循环依赖 |

# SQLite（本地开发）。生产 PostgreSQL 时改为 postgresql://... 并将 schema provider 切为 postgresql
DATABASE_URL="file:./dev.db"
# 生产环境请换成 `openssl rand -base64 32` 生成的随机串
AUTH_SECRET="dev-secret-change-me"
AUTH_URL="http://localhost:3000"
